{
  "name": "kalanconnect",
  "version": "1.0.0",
  "private": true,
  "description": "Plateforme malienne de mise en relation parents-professeurs",
  "scripts": {
    "dev:backend": "cd backend && python manage.py runserver",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:mobile": "cd mobile && npx expo start",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "db:migrate": "docker compose exec backend python manage.py migrate",
    "db:seed": "docker compose exec backend python manage.py seed_data",
    "install:all": "cd frontend && npm install && cd ../mobile && npm install",
    "start": "./scripts/start_dev.sh"
  }
}
